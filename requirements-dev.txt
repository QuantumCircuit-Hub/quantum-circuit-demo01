# Full local development environment: everything needed to run the
# scripts/ that (re)generate circuit data -- ingesting MQT Bench
# circuits, transpiling evolution trees, computing metrics -- plus the
# test suite. NOT used by the deployed Streamlit app itself; see
# requirements.txt for the minimal set that actually needs.
#
# Install with: pip install -r requirements-dev.txt

# Phase 1: minimal dependencies to parse OpenQASM 2 circuits, compute
# basic metrics, and run tests. Pinned to exact installed versions.
qiskit==2.5.2
pytest==9.1.1

# Phase 3: minimal dependencies for the interactive evolution-graph demo.
# streamlit renders the app; networkx models the evolution graph
# (nodes/edges) used by the app and its tests. No additional graph-
# rendering package (e.g. matplotlib/graphviz) was needed: the graph is
# rendered as simple left-to-right cards via Streamlit's own layout
# (st.columns/st.container), which avoids extra dependencies and avoids
# Windows console-encoding issues since it renders in the browser.
streamlit==1.63.0
networkx==3.6.1

# Phase 4A: official MQT Bench package, used to generate genuine
# benchmark circuits for ingestion. Installing it did NOT change the
# existing qiskit (2.5.2) or networkx (3.6.1) versions already pinned
# above (verified with `pip check`: no conflicts).
mqt.bench==2.2.3

# Installed automatically as transitive dependencies of the above
# (pinned for reproducibility; not installed directly).
altair==6.2.2
antlr4-python3-runtime==4.13.2
anyio==4.15.1
attrs==26.1.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
cloudpickle==3.1.2
colorama==0.4.6
dill==0.4.1
h11==0.16.0
httptools==0.8.0
idna==3.19
iniconfig==2.3.0
itsdangerous==2.2.0
Jinja2==3.1.6
joblib==1.6.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
MarkupSafe==3.0.3
narwhals==2.26.0
numpy==2.4.6
openqasm3==1.0.1
packaging==26.3
pandas==3.0.5
pillow==12.3.0
pluggy==1.6.0
protobuf==7.36.1
pyarrow==25.0.1
pydeck==0.9.3
Pygments==2.21.0
python-dateutil==2.9.0.post0
python-multipart==0.0.32
qiskit-qasm3-import==0.6.0
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
rustworkx==0.18.1
scikit-learn==1.9.1
scipy==1.17.1
six==1.17.0
starlette==1.6.0
stevedore==5.9.1
threadpoolctl==3.6.0
toml==0.10.2
typing_extensions==4.16.0
tzdata==2026.3
urllib3==2.7.0
uvicorn==0.52.4
watchdog==6.0.0
websockets==16.1.1
